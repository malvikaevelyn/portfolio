// Centralized background configuration for all pages

const backgrounds = {
  landing: {
    image: '', // Placeholder for the landing page background image
    color: '#ffffff', // landing still white by default
  },
  default: {
    color: '#ffffff', // body pages default white as requested
  },
};

export default backgrounds;
